# Supplied release and execution records

Published 1.0.0 contains single-label rendering only. No implementation or actual
output record for 1.0.0 is supplied. CSV export and the dashboard have not shipped.
W2 is an unreleased working source snapshot. The only test execution record is
for W1, one passing assertion on Node 22/Linux. No W2 execution is supplied.
There are no package scripts, commands or remote services to run for this review.
